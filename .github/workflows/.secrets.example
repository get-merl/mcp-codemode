# Example secrets file for act
# Copy this to .github/workflows/.secrets and fill in your values
# The .secrets file is gitignored and won't be committed

# GitHub token (for actions that need to interact with GitHub API)
# You can use a personal access token or GITHUB_TOKEN
GITHUB_TOKEN=ghp_your_token_here

# NPM token (for publishing packages)
# Get this from npmjs.com -> Access Tokens
NPM_TOKEN=npm_your_token_here
